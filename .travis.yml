language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t timoshuka/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true timoshuka/docker-react npm run test
